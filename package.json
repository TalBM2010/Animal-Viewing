{
  "name": "wildsnap",
  "version": "1.0.0",
  "private": true,
  "description": "WildSnap — an educational animal-discovery game where you 'capture' rare animals with your camera.",
  "scripts": {
    "install:all": "npm install && npm --prefix client install && npm --prefix server install",
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client run build",
    "preview": "npm --prefix client run preview"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
